{
  "name": "maxdash",
  "private": true,
  "version": "0.1.0",
  "description": "OSRS group max race tracker — hourly Hiscores snapshots + Vite dashboard.",
  "workspaces": [
    "scraper",
    "dashboard"
  ],
  "scripts": {
    "snapshot": "npm -w scraper run snapshot",
    "backfill": "npm -w scraper run backfill --",
    "dev": "npm -w dashboard run dev",
    "build": "npm -w dashboard run build"
  },
  "engines": {
    "node": ">=20"
  }
}
